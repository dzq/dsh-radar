{
  "slug": "dsh-plugin-credential-guard",
  "name": "dsh-plugin-credential-guard",
  "version": "0.1.0",
  "description": "DeepSeek Harness plugin: deny read/read_image/grep/glob calls whose path matches credential-store or secret-file patterns (.credentials.yaml, .env, .ssh, id_* keys, ...).",
  "keywords": [
    "dsh",
    "dsh-plugin",
    "deepseek-harness",
    "security",
    "credentials",
    "secrets"
  ],
  "license": "MIT",
  "repo": "",
  "homepage": "",
  "npm": "https://www.npmjs.com/package/dsh-plugin-credential-guard",
  "dsh_compat": false,
  "dsh_meta": null,
  "engines": {
    "node": ">=18"
  },
  "readme": "# dsh-plugin-credential-guard\n\nDeepSeek Harness 插件：拒绝模型对**凭据存储与密钥文件**的读取。\n\n## 背景\n\nharness 的文件读取在所有权限模式下都不受限（`fs-sandbox` 只拦写）。因此模型可以直接 `read` `$DSH_HOME/.credentials.yaml`、项目 `.env`、`~/.ssh` 密钥等，把密钥内容带进模型上下文。本插件在工具层关上这扇门。\n\n## 效果\n\n注册一个 `tools.guard`：`read` / `read_image` / `grep` / `glob` 的目标路径若命中敏感模式（`.credentials.yaml`、`.env`、`.ssh/`、`id_*` 密钥、`.npmrc`、`.netrc`、`.pgpass`），直接拒绝并返回 `[credential-guard] ...` 原因。\n\n## 安装\n\n```bash\ndsh plugin --profile web add dsh-plugin-credential-guard\n```\n\n挂载（profile `cordis.patch.yml` 或 agent preset）：\n\n```yaml\n- id: credential-guard\n  name: dsh-plugin-credential-guard\n  config:\n    tools: [read, read_image, grep, glob]\n    # patterns 可覆盖默认列表（见 lib/patterns.js 的 DEFAULT_PATTERNS）\n```\n\n## 边界（诚实声明）\n\n- 只拦**工具调用**（read/read_image/grep/glob 的路径参数）。`pwsh`/`bash` 里 `",
  "weekly_downloads": 0,
  "last_publish": "2026-08-14T07:18:50.988Z",
  "created_at": "2026-08-14T07:18:50.740Z",
  "gh": null,
  "scores": {
    "popularity": 0,
    "maintenance": 0,
    "quality": 75,
    "security": 50,
    "dsh_compat": 30,
    "overall": 29,
    "grade": "D"
  },
  "updated_at": "2026-08-17T04:27:46.938Z"
}