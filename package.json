{
  "name": "dsh-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DSH plugin quality radar & compatibility matrix",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "dev:data": "node scripts/build-data.mjs --watch",
    "dev:web": "cd web && pnpm dev",
    "build:web": "cd web && pnpm build",
    "build": "npm run build:data && npm run build:web"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
